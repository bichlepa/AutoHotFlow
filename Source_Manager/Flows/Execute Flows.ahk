
;~ editFlow(par_ID,e=3)
;~ {
	;~ SoundBeep
	;~ MsgBox % "edit " par_ID
;~ }

;~ enableFlow(par_ID,e=3)
;~ {
	;~ SoundBeep
	;~ MsgBox % "enableFlow " par_ID
;~ }

;~ disableFlow(par_ID,e=3)
;~ {
	;~ SoundBeep
	;~ MsgBox % "disableFlow " par_ID
;~ }

;~ toggleEnableFlow(par_ID,e=3)
;~ {
	;~ SoundBeep
	;~ MsgBox % "toggleEnableFlow " par_ID
;~ }

;~ runFlow(par_ID,e=3)
;~ {
	;~ SoundBeep
	;~ MsgBox % "runFlow " par_ID
;~ }

;~ stopFlow(par_ID,e=3)
;~ {
	;~ SoundBeep
	;~ MsgBox % "stopFlow " par_ID
;~ }

;~ toggleRunFlow(par_ID,e=3)
;~ {
	;~ SoundBeep
	;~ MsgBox % "toggleRunFlow " par_ID
;~ }
